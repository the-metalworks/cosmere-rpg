{{!-- Overrides --}}
<fieldset>
    <legend>{{localize handler.schema.fields.overrides.label}}</legend>
    {{app-advancement-overrides-list
        name="handler.overrides"
        value=handler.overrides
    }}
</fieldset>