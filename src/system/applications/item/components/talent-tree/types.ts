export interface NodeConnection {
    /**
     * The node ID of the node the connection is from
     */
    from: string;

    /**
     * The node ID of the node the connection is to
     */
    to: string;
}
